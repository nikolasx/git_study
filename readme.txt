

git的学习心得

git是一个分布式的版本管理工具。
每个人本地都有一个工作区和和本地版本库。
我们在工作区编写代码，与本地版本库发生了差异，通过commit将代码提交到本地版本库。

github提供了一个公共的代码存放库，是一个共享的版本库，通过push和fetch的方式，将本地版本库的代码
与github版本库，实现代码同步操作。

ssh是一个加密密钥，使用ssh可以免去每次和github进行代码同步时输入账号密码的步骤。

暂时的理解就是这样，估计还是有差异。